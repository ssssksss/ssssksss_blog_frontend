"use client";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "에러",
  description: "에러 페이지",
};

export default function Error() {
    return (
        <div> error </div>
    )
}